[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "envsan"
version = "0.1.0"
description = "Audit environment variables for secrets, unsafe values, and policy violations."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Henry Samuel" }]
keywords = ["markdown", "lint", "clean", "tidy"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.11",
  "Typing :: Typed",
]
dependencies = []

[project.urls]
Repository = "https://github.com/Henry-Samuel/envsan"

[project.optional-dependencies]
dev = ["pytest>=7"]

[project.scripts]
envsan = "envsan.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
